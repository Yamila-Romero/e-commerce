/*import { Types } from "mongoose";

export interface IProduct {
  _id?: Types.ObjectId;  // ID opcional si se está creando un nuevo producto
  name: string;
  description: string;
  stock: number;
  price: number;
  category: Types.ObjectId;  // Referencia a la categoría
  image?: string;  // La imagen es opcional porque tiene un valor por defecto
}
*/